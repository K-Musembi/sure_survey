package com.survey_engine.billing.models.enums;

/**
 * Enum representing the billing interval for a subscription plan.
 */
public enum PlanInterval {
    /**
     * The plan is billed on a monthly basis.
     */
    MONTHLY,

    /**
     * The plan is billed on a yearly basis.
     */
    YEARLY
}