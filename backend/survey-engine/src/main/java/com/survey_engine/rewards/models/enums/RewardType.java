package com.survey_engine.rewards.models.enums;

/**
 * Defines the types of rewards that can be offered for completing a survey.
 */
public enum RewardType {
    AIRTIME,
    DATA_BUNDLE,
    LOYALTY_POINTS,
    VOUCHER
}