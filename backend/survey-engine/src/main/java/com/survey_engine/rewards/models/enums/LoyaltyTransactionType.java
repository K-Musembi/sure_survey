package com.survey_engine.rewards.models.enums;

/**
 * Defines the type of a loyalty transaction, indicating whether points are being added or removed.
 */
public enum LoyaltyTransactionType {
    CREDIT,
    DEBIT
}