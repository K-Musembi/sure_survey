package com.survey_engine.rewards.models.enums;

/**
 * Defines a loyalty transaction type, indicating whether points are being added or removed.
 */
public enum LoyaltyTransactionType {
    CREDIT,
    DEBIT
}